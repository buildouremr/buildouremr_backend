spring.application.name=product

# DATABASE
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/smartemr}
spring.datasource.username=${DB_USERNAME:zacariya236}
spring.datasource.password=${DB_PASSWORD:smartemr2026}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# REDIS CONFIG
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# SECURITY (disable default)
# Spring Security Settings
# (No hardcoded credentials used, managed by DB and JWT)

# CONNECTION POOL
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.max-lifetime=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.keepalive-time=15000
spring.datasource.hikari.connection-test-query=SELECT 1

# EMAIL
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:teambuildurskill@gmail.com}
spring.mail.password=${MAIL_PASSWORD:usdm rsvb czup vnrf}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT CONFIGURATION
jwt.secret=${JWT_SECRET:c2VjdXJlLWVtci1wcm9kdWN0aW9uLWtleS0yMDI2LXZlcnktbG9uZy1hbmQtc2VjdXJl}
jwt.expiration=3600000
jwt.cookie.name=EMR_AUTH_TOKEN
jwt.cookie.max-age=3600
jwt.cookie.secure=${JWT_COOKIE_SECURE:false}
jwt.cookie.domain=${JWT_COOKIE_DOMAIN:localhost}

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

# SMS (Fast2SMS)
sms.api.key=${SMS_API_KEY:YOUR_API_KEY}